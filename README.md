# Homelab Docker

### Traefik
##### For network isolation
We are creating a public and a private network using traefik
```docker
docker network create traefik_public
docker network create traefik_private
```
